#################
### NVIDIA Fix ###
#################
# NVIDIA RTX 3060 - Wayland configuration
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1

# Cursor sizes for better compatibility
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# Java application fix for Wayland
env = _JAVA_AWT_WM_NONREPARENTING,1

# Electron/Chromium fixes
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = ELECTRON_DISABLE_GPU,0
env = DISABLE_LAYER_ASHMEM,1

#################
### RENDER NVIDIA ###
#################
# NVIDIA rendering options (v0.54.3 compatible)
opengl {
    nvidia_anti_flicker = true
}

misc {
    vfr = false
}

###############
### Monitor ###
###############
monitor=,preferred,auto,1


#############
### Start ###
#############
exec-once = export XDG_RUNTIME_DIR=/run/user/$(id -u)

# Wallpaper 
exec-once = ~/.config/hypr/scripts/wallpaper_v2.sh

# Input method
exec-once = fcitx5
exec-once = ~/.config/waybar/scripts/fcitx5-watcher.sh

# Notifications
exec-once = mako

# Network Manager applet
exec-once = nm-applet --indicator

# Waybar
exec-once = waybar &

# Eww widgets and bar
exec-once = sleep 2 && cd ~/.config/eww && eww daemon && sleep 1 && eww open dashboard_window && eww open date_window

# Session idle & lock
exec-once = swayidle \
    timeout 900 'hyprlock' \
    before-sleep 'hyprlock'

###################
### KEYBINDINGS ###
###################

# Terminal
bind = SUPER, R, exec, alacritty
# Browser (Brave)
bind = SUPER, E, exec, brave
# File Explorer (Thunar)
bind = SUPER, F, exec, thunar
# Discord
bind = SUPER, C, exec, ~/.config/hypr/scripts/discord.sh
# Steam
bind = SUPER, S, exec, steam
# Blender
bind = SUPER, B, exec, blender
# App Launcher
bind = SUPER, D, exec, ~/.config/wofi/wofi-launch.sh --show drun

# Screenshot - Fullscreen
bind = SUPERSHIFT, 3, exec, grim ~/Pictures/screenshot_$(date +%F_%T).png
# Screenshot - Region
bind = SUPERSHIFT, 4, exec, grim -g "$(slurp)" ~/Pictures/screenshot_$(date +%F_%T).png
# Screen recording with OBS (toggle recording on/off)
bind = SUPERSHIFT, 5, exec, ~/.config/hypr/scripts/obs-recording.sh
# Stop screen recording (same as toggle)
bind = SUPERSHIFT, 6, exec, ~/.config/hypr/scripts/obs-recording.sh

# Close window
bind = SUPER, Q, killactive
# Exit Hyprland
bind = SUPER, M, exit

# Brightness
bind = ,XF86MonBrightnessUp, exec, brightnessctl s +10%
bind = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Audio
bind = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Notes - Open text editor
bind = SUPER, N, exec, alacritty -e nano ~/.local/share/kuri-notes/nota.txt

# Move focus with mod + arrow keys
bind = SUPER, H, movefocus, l
bind = SUPER, L, movefocus, r
bind = SUPER, K, movefocus, u
bind = SUPER, J, movefocus, d

# Switch workspaces with mod + [0-9]
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9

# Switch language/input method
bind = SUPER, 0, exec, fcitx5-remote -t

# Move active window to a workspace with mod + SHIFT + [0-9]
# bind = SUPERSHIFT, 1, movetoworkspace, 1
# bind = SUPERSHIFT, 2, movetoworkspace, 2
# bind = SUPERSHIFT, 3, movetoworkspace, 3
# bind = SUPERSHIFT, 4, movetoworkspace, 4
# bind = SUPERSHIFT, 5, movetoworkspace, 5
# bind = SUPERSHIFT, 6, movetoworkspace, 6
# bind = SUPERSHIFT, 7, movetoworkspace, 7
# bind = SUPERSHIFT, 8, movetoworkspace, 8
# bind = SUPERSHIFT, 9, movetoworkspace, 9
# bind = SUPERSHIFT, 0, movetoworkspace, 10

###################
### Decoration ###
###################
decoration {
    rounding = 8
    active_opacity = 1.0
    inactive_opacity = 1.0

    # Blur settings
    blur {
        enabled = true
        size = 1
        passes = 3
        new_optimizations = false
        ignore_opacity = false
        xray = false
        popups = true
    }
}

###################
### Animations ###
###################
animations {
    enabled = false
}

###################
### General ###
###################
general {
    border_size = 3
    col.active_border = rgba(0,255,136,0.6) rgba(0,255,180,0.9) 45deg
    col.inactive_border = rgba(0,255,136,0.15) rgba(0,255,180,0.25) 45deg
}

###################
### Misc ###
###################
misc {
  disable_hyprland_logo = true
  disable_splash_rendering = true
}

# Lock screen
bind = SUPERSHIFT, L, exec, hyprlock
